# apps/api specific settings. Root .env.example covers values shared with
# other services (DATABASE_URL, MUSIC_PROVIDER, VIBE_MODEL_PATH).

API_HOST=0.0.0.0
API_PORT=8000

# CORS — the Next.js dev server origin
CORS_ORIGINS=http://localhost:3000
