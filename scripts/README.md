# scripts

Home for developer/data utility scripts that don't belong inside a specific
app or package — e.g. local environment setup, dataset download/prep
helpers, or one-off migration scripts.

Empty for now; add scripts here as they're needed rather than
speculatively.
